# Retrieval-Augmented Generation (RAG)
# Retrieval-Augmented Generation (RAG)

RAG systems inject external custom data into an LLM's context. This relies on semantic search, where documents are chunked, embedded, stored in vector databases, retrieved based on the user's query, and passed to the LLM to generate an informed response.

