# Supervised Fine-Tuning (SFT) and Instruction Tuning
# Supervised Fine-Tuning (SFT) and Instruction Tuning

The pretrained base model is further trained on smaller, task-specific, labeled datasets (instruction-answer pairs) to make it useful for specific formats like chatbots or text classifiers. 

